import React from "react";
import Header from "./Header";

class Resources extends React.Component {
  render() {
    return (
      <div>
        <Header />
        <div>Recources page (it's super secret)</div>
      </div>
    );
  }
}

export default Resources;
